server:
  port: 8080

spring:
  jpa:
    show-sql: false
    properties:
      hibernate.hibernate.format_sql: true

email.redirect.base-url: http://localhost:5173

management:
  otlp:
    metrics:
      export:
        enabled: false
