server:
  port: 0

spring:
  jpa:
    hibernate:
      ddl-auto: none
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://test.auth.com

supabase:
  url: https://test.auth.com
  service-role-key: secret-key

stripe:
  secret-key: test-key
  whsec-connected-account: test-whsec
  whsec-account: test-whsec

member-access-qr:
  private-key: ${MEMBER_ACCESS_QR_PRIVATE_KEY}
  expiration-seconds: 10

notifications:
  telegram:
    bot.token: bot-token
    chat.id: chat-id
